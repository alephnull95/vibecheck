version: '3.8'

services:
  vibecheck:
    build: .
    container_name: vibecheck
    ports:
      - "5050:5000"
    environment:
      - FLASK_ENV=production
      - PYTHONUNBUFFERED=1
    restart: always
